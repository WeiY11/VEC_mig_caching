# 🎯 Baseline对比实验 - 改进功能说明

**更新日期**: 2025-10-10  
**版本**: v2.0（完善版）

---

## 🆕 新增功能

### 1. ✅ 固定拓扑支持
- **固定配置**: 4 RSU + 2 UAV
- **可变车辆数**: 支持通过 `--num-vehicles` 参数指定
- **公平对比**: 所有算法在相同拓扑下运行

### 2. ✅ 改进的Baseline算法
- **正确动作维度**: 16维（3+4+2+7）而非旧版的18维
- **智能状态解析**: 自动适配不同车辆数的状态维度
- **文件**: `improved_baseline_algorithms.py`

### 3. ✅ 复合指标分析
- **目标函数**: J = 2.0×Delay + 1.2×Energy
- **能耗归一化**: Energy/600以匹配时延数量级
- **自动排名**: 按目标函数值排序所有算法

### 4. ✅ 统计显著性检验
- **方法**: t-test（独立双样本）
- **对比**: 所有算法 vs TD3
- **显著性标记**: *** (p<0.001), ** (p<0.01), * (p<0.05)
- **需求**: scipy库（如未安装会自动跳过）

### 5. ✅ 增强的可视化
- **性能对比图**: 时延、能耗、完成率（3个子图）
- **目标函数对比图**: 复合指标排名（新增）
- **收敛曲线图**: DRL算法训练过程（4个子图）
- **高分辨率**: 300 DPI，适合论文使用

### 6. ✅ 灵活的运行模式
- **完整运行**: 所有DRL + 所有Baseline
- **只运行DRL**: `--only-drl`
- **只运行Baseline**: `--only-baseline`
- **单独算法**: `--algorithm <名称>`
- **多seed运行**: `--multi-seed <次数>`

---

## 📋 使用方法

### 基本用法

#### 快速测试（50轮，~30分钟）
```bash
python run_baseline_comparison.py --quick
```

#### 标准对比（200轮，~2-3小时）
```bash
python run_baseline_comparison.py --episodes 200
```

#### 完整实验（500轮，~5-6小时）
```bash
python run_baseline_comparison.py --full
```

### 高级用法

#### 指定车辆数（固定拓扑）
```bash
python run_baseline_comparison.py --episodes 200 --num-vehicles 16
```

#### 只运行DRL算法
```bash
python run_baseline_comparison.py --episodes 200 --only-drl
```

#### 只运行启发式算法
```bash
python run_baseline_comparison.py --episodes 100 --only-baseline
```

#### 单独测试某个算法
```bash
# 测试TD3
python run_baseline_comparison.py --algorithm TD3 --episodes 100

# 测试Greedy
python run_baseline_comparison.py --algorithm Greedy --episodes 100
```

#### 多seed运行（增强可靠性）
```bash
python run_baseline_comparison.py --episodes 200 --multi-seed 3
# 将运行3次实验，种子为42, 142, 242
```

---

## 📊 输出结果

### 数据文件
- `results/<算法名>/result_<算法名>.json` - 每个算法的详细结果
- `results/comparison_summary_<时间戳>.json` - 所有算法汇总

### 图表文件（analysis/目录）
1. **performance_comparison.png** - 三指标性能对比
   - 平均任务时延
   - 系统总能耗
   - 任务完成率

2. **objective_comparison.png** - 目标函数对比（新增⭐）
   - 复合指标：J = 2.0×Delay + 1.2×Energy
   - 突出显示TD3
   - 相对性能标签

3. **convergence_curves.png** - DRL算法收敛曲线
   - 时延收敛
   - 能耗收敛
   - 完成率收敛
   - 奖励收敛

### 终端输出
1. **性能对比表** - 各算法的时延、能耗、完成率
2. **复合指标分析** - 目标函数值排名
3. **统计显著性检验** - t-test结果和p-value

---

## 🔬 统计显著性检验说明

### 检验方法
- **t-test**: 独立双样本t检验（双侧）
- **对比基准**: TD3
- **数据范围**: 稳定期（后50% episodes）

### 显著性标准
| 符号 | p-value范围 | 含义 |
|------|------------|------|
| *** | p < 0.001 | 极显著 |
| ** | p < 0.01 | 高度显著 |
| * | p < 0.05 | 显著 |
| n.s. | p ≥ 0.05 | 不显著 |

### 解读示例
```
算法            时延p-value     能耗p-value     显著性
Random          0.000012        0.000003        ***/**
Greedy          0.023000        0.041000        */n.s.
```
- **Random**: 时延和能耗均极显著差于TD3
- **Greedy**: 时延显著差于TD3，能耗差异不显著

---

## 🎯 实验建议

### 论文级实验（推荐）
```bash
# 标准配置（12辆车）
python run_baseline_comparison.py --episodes 200 --seed 42

# 预计时间：
# - DRL算法: 5个 × 200轮 × 1.5秒/轮 ≈ 25分钟
# - Baseline算法: 5个 × 200轮 × 0.5秒/轮 ≈ 8分钟
# - 总计: ~35-40分钟
```

### 高可靠性实验（多seed）
```bash
# 3个不同随机种子
python run_baseline_comparison.py --episodes 200 --multi-seed 3

# 预计时间：~2小时
```

### 快速验证（测试修改）
```bash
# 快速模式
python run_baseline_comparison.py --quick

# 只测试DRL
python run_baseline_comparison.py --quick --only-drl

# 预计时间：~5-10分钟
```

---

## 📈 关键改进点

### 动作维度修复 ✅
```
旧版: 18维动作（错误）
新版: 16维动作（正确，适配固定拓扑4 RSU+2 UAV）
```

### 状态解析优化 ✅
```python
# 智能适配不同车辆数的状态维度
num_vehicles = 12  # 可变
state_dim = num_vehicles*5 + 4*5 + 2*5 + 8  # 动态计算
```

### 复合指标引入 ✅
```
旧版: 只对比单个指标（时延、能耗）
新版: 复合指标 J = 2.0×Delay + 1.2×Energy（论文核心）
```

### 统计检验添加 ✅
```
旧版: 无统计检验
新版: t-test + p-value + 显著性标记（学术规范）
```

---

## 🔧 技术细节

### 改进的Baseline算法
1. **RandomBaseline**: 16维随机动作
2. **GreedyBaseline**: 负载最小优先 + 智能状态解析
3. **RoundRobinBaseline**: 轮询分配 + 固定拓扑适配
4. **LocalFirstBaseline**: 本地优先（过载阈值0.8）
5. **NearestNodeBaseline**: 距离优先 + 负载过滤

### 环境配置
- 通过环境变量 `TRAINING_SCENARIO_OVERRIDES` 设置车辆数
- 通过环境变量 `RANDOM_SEED` 确保可复现性
- 固定拓扑：RSU=4, UAV=2（不受车辆数影响）

---

## ✅ 验证清单

在运行完整实验前，建议验证：

- [ ] 改进的baseline算法工作正常
  ```bash
  python run_baseline_comparison.py --algorithm Greedy --episodes 10
  ```

- [ ] TD3在固定拓扑下正常训练
  ```bash
  python run_baseline_comparison.py --algorithm TD3 --episodes 20 --num-vehicles 12
  ```

- [ ] 统计检验功能可用
  ```bash
  pip install scipy  # 如果未安装
  ```

- [ ] 可视化系统正常
  - 检查是否生成3个PNG文件

---

## 🎓 论文使用建议

### 图表选择
1. **主图**: `objective_comparison.png` - 展示TD3的综合优势
2. **辅助图**: `performance_comparison.png` - 展示各指标详细对比
3. **补充图**: `convergence_curves.png` - 展示DRL算法的学习过程

### 表格数据
使用终端输出的表格：
- 性能对比表（包含均值±标准差）
- 复合指标排名表
- 统计显著性表（p-value）

### 文字描述模板
```
"实验结果表明，TD3算法在目标函数J=2.0×Delay+1.2×Energy上
显著优于所有对比算法（p<0.001）。相比最佳启发式算法Greedy，
TD3在时延上降低了X%，在能耗上降低了Y%，综合性能提升Z%。"
```

---

## 🚀 快速开始

### 第一步：环境准备
```bash
cd baseline_comparison
pip install scipy  # 用于统计检验（可选）
```

### 第二步：快速验证
```bash
python run_baseline_comparison.py --quick
# 确认所有功能正常
```

### 第三步：完整实验
```bash
python run_baseline_comparison.py --episodes 200
# 获取论文级数据
```

---

## 💡 常见问题

**Q: 为什么Baseline算法也使用TD3环境？**  
A: 为了保证公平对比，所有算法应在相同的环境下运行。TD3环境提供完整的系统仿真。

**Q: 如何选择合适的车辆数？**  
A: 建议使用12辆车（默认配置），这是所有算法的最优配置点。

**Q: 统计检验不可用怎么办？**  
A: 安装scipy即可：`pip install scipy`。如不安装，统计检验会自动跳过。

**Q: 多seed运行有什么用？**  
A: 增强实验可靠性，减少随机性影响，更符合学术规范。

---

## 📝 改进日志

**v2.0 (2025-10-10)**:
- ✅ 添加固定拓扑支持
- ✅ 修复动作维度（18→16）
- ✅ 添加复合指标分析
- ✅ 添加统计显著性检验
- ✅ 改进可视化系统
- ✅ 支持多seed运行
- ✅ 添加灵活的运行模式

**v1.0 (初始版本)**:
- 基本的算法对比功能
- 简单的可视化


