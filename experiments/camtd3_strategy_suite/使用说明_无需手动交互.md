# CAMTD3 对比实验 - 无需手动交互使用指南

## 🎯 核心要点

所有批量对比实验脚本**现已默认静默运行**，无需任何手动交互！

- ✅ 自动保存所有训练报告
- ✅ 无人值守长时间运行
- ✅ 适合服务器后台批量实验

## 📋 所有实验脚本快速命令

### 1. 带宽敏感性分析
```bash
# 默认静默运行（推荐）
python experiments/camtd3_strategy_suite/run_bandwidth_cost_comparison.py \
    --episodes 500 --seed 42 --suite-id bandwidth_six_strategies
```

### 2. 数据大小敏感性分析
```bash
# 默认静默运行（推荐）
python experiments/camtd3_strategy_suite/run_data_size_comparison.py \
    --episodes 500 --seed 42 --suite-id datasize_paper
```

### 3. 边缘节点配置分析
```bash
# 默认静默运行（推荐）
python experiments/camtd3_strategy_suite/run_edge_node_comparison.py \
    --episodes 500 --seed 42 --suite-id edge_paper
```

### 4. 本地资源成本分析
```bash
# 默认静默运行（推荐）
python experiments/camtd3_strategy_suite/run_local_resource_cost_comparison.py \
    --episodes 500 --seed 42 --suite-id local_paper
```

### 5. 本地资源卸载对比
```bash
# 默认静默运行（推荐）
python experiments/camtd3_strategy_suite/run_local_resource_offload_comparison.py \
    --episodes 500 --seed 42 --suite-id offload_paper
```

### 6. 任务到达率分析
```bash
# 默认静默运行（推荐）
python experiments/camtd3_strategy_suite/run_task_arrival_comparison.py \
    --episodes 500 --seed 42 --suite-id arrival_paper
```

### 7. 车辆数量敏感性分析
```bash
# 默认静默运行（推荐）
python experiments/camtd3_strategy_suite/run_vehicle_count_comparison.py \
    --episodes 500 --seed 42 --suite-id vehicle_paper
```

### 8. 移动速度敏感性分析
```bash
# 默认静默运行（推荐）
python experiments/camtd3_strategy_suite/run_mobility_speed_comparison.py \
    --episodes 500 --seed 42 --suite-id mobility_paper
```

### 9. 策略多场景对比
```bash
# 默认静默运行（推荐）
python experiments/camtd3_strategy_suite/run_strategy_context_comparison.py \
    --episodes 500 --seed 42 --suite-id context_paper
```

### 10. 批量实验管理器
```bash
# 默认静默运行（推荐）
python experiments/camtd3_strategy_suite/run_batch_experiments.py \
    --mode full --all
```

## 💡 如需交互模式

如果您希望手动确认是否保存报告（交互模式），只需添加 `--interactive` 参数：

```bash
python experiments/camtd3_strategy_suite/run_bandwidth_cost_comparison.py \
    --episodes 500 --seed 42 --interactive
```

## 📊 快速测试模式

所有脚本都支持快速测试模式（减少训练轮数）：

```bash
# 100轮快速测试
python experiments/camtd3_strategy_suite/run_bandwidth_cost_comparison.py \
    --episodes 100 --suite-id bandwidth_quick

# 50轮极速测试
python experiments/camtd3_strategy_suite/run_data_size_comparison.py \
    --episodes 50 --suite-id datasize_test
```

## 🚀 批量运行所有实验

使用批量管理器一次性运行所有实验：

```bash
# 快速模式（每个实验10轮）
python experiments/camtd3_strategy_suite/run_batch_experiments.py --mode quick --all

# 中等模式（每个实验50轮）
python experiments/camtd3_strategy_suite/run_batch_experiments.py --mode medium --all

# 完整模式（每个实验500轮，论文级别）
python experiments/camtd3_strategy_suite/run_batch_experiments.py --mode full --all
```

## 📁 结果保存位置

所有实验结果默认保存在：

- 参数敏感性实验：`results/parameter_sensitivity/`
- 策略对比实验：`results/camtd3_strategy_suite/`
- HTML训练报告：`results/single_agent/{algorithm}/`

每次实验都会生成：
- ✅ 性能指标JSON文件
- ✅ 对比图表PNG文件
- ✅ HTML详细报告
- ✅ 汇总summary.json

## ⏱️ 预计运行时间

| 实验类型 | 快速测试(100轮) | 完整实验(500轮) |
|---------|----------------|----------------|
| 单个参数对比 | 1.5-3小时 | 6-10小时 |
| 多参数批量 | 3-6小时 | 12-24小时 |
| 全部实验 | 10-20小时 | 2-4天 |

## 🔍 查看进度

运行时会实时显示：
- ✅ 当前配置信息
- ✅ 策略训练进度
- ✅ 性能指标输出
- ✅ 完成百分比

## ❓ 常见问题

### Q: 如何中断实验？
A: 按 `Ctrl+C` 中断。已完成的结果会被保存。

### Q: 可以在后台运行吗？
A: 可以！在Linux/Mac上使用 `nohup` 或 `screen`，Windows上直接最小化窗口。

### Q: 如何并行运行多个实验？
A: 使用 `--parallel` 参数（需要多GPU支持）：
```bash
python experiments/camtd3_strategy_suite/run_batch_experiments.py \
    --mode full --all --parallel 3
```

### Q: 实验失败怎么办？
A: 检查错误日志，修复问题后可以指定未完成的实验继续运行：
```bash
python experiments/camtd3_strategy_suite/run_batch_experiments.py \
    --mode full --experiments 3,5,7
```

## 📚 更多信息

- 技术细节：查看 `SILENT_MODE_FIX.md`
- 脚本说明：查看各脚本头部的详细注释
- 配置选项：运行 `python <script>.py --help`

---

**最后更新**: 2024-10-30  
**修改内容**: 所有批量实验脚本默认启用静默模式，无需手动交互


