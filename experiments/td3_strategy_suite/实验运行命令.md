# TD3 策略对比实验 - 运行命令文档

## 📑 实验索引

| 编号 | 实验名称 | 类别 | 参数配置 | 配置数 |
|-----|---------|------|---------|-------|
| 1 | 车辆数量对比 | 系统规模 | 8, 12, 16 辆 | 3 |
| 2 | 边缘节点配置对比 | 系统规模 | 3种RSU/UAV组合 | 3 |
| 3 | 网络与拓扑综合对比⭐ | 系统规模 | 6个综合场景 | 6 |
| 4 | 移动速度对比 | 系统规模 | 10, 20, 30 m/s | 3 |
| 5 | 任务到达率对比 | 任务特性 | 0.8, 1.2, 1.6 tasks/s | 3 |
| 6 | 任务复杂度对比 | 任务特性 | 0.8×, 1.2×, 1.8× | 3 |
| 7 | 数据大小对比 | 任务特性 | 小/中/大 | 3 |
| 8 | 本地计算资源综合对比⭐ | 资源配置 | 1.2, 2.0, 2.8 GHz | 3 |
| 9 | 边缘基础设施综合对比⭐ | 资源配置 | 5个综合场景 | 5 |
| 10 | 缓存容量对比 | 资源配置 | 256, 512, 1024 MB | 3 |
| 11 | 混合负载场景对比 | 综合场景 | 计算密集型 | 1 |
| 12 | Pareto权重分析 | 综合场景 | 7种权重配置 | 7 |
| 13 | 服务能力扩展对比 | 其他 | 0.6×, 1.0×, 1.4× | 3 |
| 14 | 资源异构性对比 | 其他 | 异构配置 | 1 |

**总计**：14个实验，47个配置  
**说明**：⭐ 标记的是合并实验（整合了2-3个原始实验）

---

## 📋 批量运行工具

### 1. 批量运行所有实验
```bash
# 快速测试（10轮）- 所有14个实验
python experiments/td3_strategy_suite/run_batch_experiments.py --mode quick --all

# 中等测试（100轮）- 指定实验（使用实验编号）
python experiments/td3_strategy_suite/run_batch_experiments.py --mode medium --experiments 1,2,6,7,8

# 核心实验（高优先级实验：1,2,3,5,9,10,11,12）
python experiments/td3_strategy_suite/run_batch_experiments.py --mode medium --experiments 1,2,3,5,9,10,11,12

# 完整实验（500轮）- 所有实验
python experiments/td3_strategy_suite/run_batch_experiments.py --mode full --all

# 并行运行（多GPU）
python experiments/td3_strategy_suite/run_batch_experiments.py --mode medium --parallel 3 --all
```

**实验编号说明**：使用 `--experiments` 参数指定要运行的实验编号（见上方索引表）

### 2. 策略套件运行（消融实验）
```bash
# 运行所有TD3策略变体
python experiments/td3_strategy_suite/run_full_suite.py

# 运行指定策略
python experiments/td3_strategy_suite/run_full_suite.py --strategies full,no_migration,no_cache
```

---

## 🔬 单独对比实验（14个）

### 📊 系统规模与拓扑（4个）

#### 实验1：车辆数量对比
```bash
python experiments/td3_strategy_suite/run_vehicle_count_comparison.py
```
**对比**：不同车辆数量下的策略性能  
**参数**：8, 12, 16 辆（小/中/大规模）

#### 实验2：边缘节点配置对比
```bash
python experiments/td3_strategy_suite/run_edge_node_comparison.py
```
**对比**：不同边缘节点配置下的策略性能  
**参数**：(2 RSU, 0 UAV), (4 RSU, 2 UAV), (6 RSU, 3 UAV)

#### 实验3：网络与拓扑综合对比
```bash
python experiments/td3_strategy_suite/run_network_topology_comparison.py
```
**对比**：带宽、信道质量、拓扑密度的综合影响  
**参数**：6个综合场景（🔄 合并了带宽、信道质量、拓扑密度3个实验）
1. **最差场景**：10MHz带宽 + 差信道(-90dBm) + 稀疏拓扑(2 RSU, 1 UAV)
2. **带宽瓶颈**：10MHz带宽 + 好信道(-110dBm) + 密集拓扑(6 RSU, 3 UAV)
3. **信道瓶颈**：50MHz带宽 + 差信道(-90dBm) + 稀疏拓扑(2 RSU, 1 UAV)
4. **拓扑瓶颈**：50MHz带宽 + 好信道(-110dBm) + 稀疏拓扑(2 RSU, 1 UAV)
5. **标准场景**：30MHz带宽 + 中信道(-100dBm) + 标准拓扑(4 RSU, 2 UAV)
6. **最优场景**：50MHz带宽 + 好信道(-110dBm) + 密集拓扑(6 RSU, 3 UAV)

#### 实验4：移动速度对比
```bash
python experiments/td3_strategy_suite/run_mobility_speed_comparison.py
```
**对比**：不同车辆移动速度下的策略性能  
**参数**：10, 20, 30 m/s（慢速/中速/快速）

---

### 📦 任务特性（3个）

#### 实验5：任务到达率对比
```bash
python experiments/td3_strategy_suite/run_task_arrival_comparison.py
```
**对比**：不同任务到达率下的策略性能  
**参数**：0.8, 1.2, 1.6 tasks/s（低/中/高负载）

#### 实验6：任务复杂度对比
```bash
python experiments/td3_strategy_suite/run_task_complexity_comparison.py
```
**对比**：不同任务计算复杂度下的策略性能  
**参数**：0.8×, 1.2×, 1.8× 基准复杂度（简单/中等/复杂）

#### 实验7：数据大小对比
```bash
python experiments/td3_strategy_suite/run_data_size_comparison.py
```
**对比**：不同任务数据大小下的策略性能  
**参数**：3个数据大小配置（小/中/大）

---

### 💻 资源配置（3个）

#### 实验8：本地计算资源综合对比
```bash
python experiments/td3_strategy_suite/run_local_compute_resource_comparison.py
```
**对比**：本地资源成本和卸载能力的综合影响  
**参数**：3个CPU频率等级（🔄 合并了本地资源成本、本地资源卸载2个实验）
- **低频**：1.2 GHz（本地计算慢，倾向卸载）
- **中频**：2.0 GHz（均衡配置，标准场景）
- **高频**：2.8 GHz（本地计算快，减少卸载）

#### 实验9：边缘基础设施综合对比
```bash
python experiments/td3_strategy_suite/run_edge_infrastructure_comparison.py
```
**对比**：边缘计算能力和通信资源的综合影响  
**参数**：5个综合场景（🔄 合并了边缘计算能力、边缘通信资源2个实验）
1. **低计算+低通信**：RSU 10GHz/UAV 8GHz + 15MHz（资源受限）
2. **高计算+低通信**：RSU 20GHz/UAV 16GHz + 15MHz（通信瓶颈）
3. **低计算+高通信**：RSU 10GHz/UAV 8GHz + 40MHz（计算瓶颈）
4. **均衡配置**：RSU 15GHz/UAV 12GHz + 20MHz（标准场景）
5. **高计算+高通信**：RSU 20GHz/UAV 16GHz + 40MHz（资源充足）

#### 实验10：缓存容量对比
```bash
python experiments/td3_strategy_suite/run_cache_capacity_comparison.py
```
**对比**：不同缓存容量下的策略性能  
**参数**：256, 512, 1024 MB（小/中/大）

---

### 🔀 综合场景（2个）

#### 实验11：混合负载场景对比
```bash
python experiments/td3_strategy_suite/run_mixed_workload_comparison.py
```
**对比**：混合任务类型下的策略性能  
**参数**：计算密集型场景（单一配置）

#### 实验12：Pareto权重分析
```bash
python experiments/td3_strategy_suite/run_pareto_weight_analysis.py
```
**对比**：不同时延-能耗权重组合的策略性能  
**参数**：7种权重配置（时延权重, 能耗权重）- 覆盖不同应用场景

| 配置名称 | 权重(时延, 能耗) | 应用场景 | 特点 |
|---------|-----------------|---------|------|
| 极限时延 | (4.0, 0.2) | 自动驾驶 | 超低时延优先 |
| 时延优先 | (3.0, 0.5) | 远程医疗 | 时延敏感 |
| 偏时延 | (2.5, 1.0) | 在线游戏 | 时延略重 |
| 均衡 | (2.0, 1.2) | 标准配置 | 时延能耗平衡 |
| 偏能耗 | (1.5, 2.0) | 物联网 | 能耗略重 |
| 能耗优先 | (0.5, 3.0) | 电池设备 | 能耗敏感 |
| 极限节能 | (0.2, 4.0) | 传感器 | 极限节能 |

---

### 📌 其他实验（2个）

#### 实验13：服务能力扩展对比
```bash
python experiments/td3_strategy_suite/run_service_capacity_comparison.py
```
**对比**：不同服务能力扩展场景下的策略性能  
**参数**：0.6×, 1.0×, 1.4× 基准服务能力（低/基准/高）

#### 实验14：资源异构性对比
```bash
python experiments/td3_strategy_suite/run_resource_heterogeneity_comparison.py
```
**对比**：资源异构性对策略性能的影响  
**参数**：单一异构配置（需查看脚本详情）

---

## ⚙️ 通用参数

所有实验脚本都支持以下参数：

```bash
--episodes N        # 训练轮数（默认100）
--seed N           # 随机种子（默认42）
--suite-id NAME    # 实验套件ID（默认自动生成时间戳）
--silent           # 静默模式（默认开启）
--no-silent        # 详细输出模式
```

**示例**：
```bash
# 指定轮数和种子
python experiments/td3_strategy_suite/run_vehicle_count_comparison.py --episodes 200 --seed 123

# 详细输出模式
python experiments/td3_strategy_suite/run_data_size_comparison.py --no-silent
```

---

## 📂 结果输出

所有实验结果保存在 `results/td3_strategy_suite/` 目录下：
- 训练曲线图（PNG格式）
- 性能对比表（JSON格式）
- 详细日志文件

---

## 💡 快速开始建议

1. **快速验证**：先跑1-2个实验的quick模式（10轮）
   ```bash
   python experiments/td3_strategy_suite/run_batch_experiments.py --mode quick --experiments 1,5
   ```

2. **核心实验**：运行高优先级实验（实验1,2,3,5,9,10,11,12）
   ```bash
   python experiments/td3_strategy_suite/run_batch_experiments.py --mode medium --experiments 1,2,3,5,9,10,11,12
   ```

3. **完整实验**：运行所有实验（建议夜间无人值守）
   ```bash
   python experiments/td3_strategy_suite/run_batch_experiments.py --mode full --all
   ```

---

## 📊 实验规模统计

- **实验总数**：14个
- **配置总数**：47个
- **策略数量**：6种（full, no_migration, no_cache, no_collaboration, local_only, random）
- **预估时间**（100轮/配置）：
  - 快速模式（10轮）：约10-20分钟/实验
  - 中等模式（100轮）：约1.5-3小时/实验
  - 完整模式（500轮）：约7-15小时/实验

---

**文档版本**: v2.0  
**更新日期**: 2025-11-04  
**新增内容**：详细参数配置说明、实验索引表

