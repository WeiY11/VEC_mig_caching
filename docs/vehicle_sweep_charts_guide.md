# 🚗 车辆数扫描实验 - 自动图表生成指南

**版本**: v1.0  
**日期**: 2025-10-08

---

## 🎯 功能概述

`run_td3_vehicle_sweep.py`脚本现已支持**自动生成对比图表**！

当你运行：
```bash
python experiments/run_td3_vehicle_sweep.py --vehicles 8 12 16 20 24 --episodes 800
```

脚本会：
1. ✅ 依次训练不同车辆数量的TD3模型
2. ✅ 记录所有关键指标
3. ✅ **自动生成6个专业对比图表**（300 DPI）
4. ✅ 保存JSON和Markdown总结

---

## 📊 自动生成的图表（共2个文件）

### 图表1：综合对比图（6个子图）

**文件名**: `vehicle_sweep_comparison_{timestamp}.png`  
**尺寸**: 16×12英寸，300 DPI  
**格式**: 2×3布局

#### 包含的子图：

1. **(a) 平均时延 vs 车辆数**
   - 展示车辆数量如何影响系统时延
   - 红色曲线，带数值标注

2. **(b) 任务完成率 vs 车辆数**
   - 展示不同车辆数的任务完成情况
   - 绿色曲线，百分比标注

3. **(c) 平均每步奖励 vs 车辆数**
   - 展示训练性能随车辆数的变化
   - 蓝色曲线

4. **(d) 训练时间 vs 车辆数**
   - 展示计算复杂度随规模的增长
   - 紫色曲线

5. **(e) 收敛性对比**
   - 多条不同颜色的收敛曲线
   - 直观对比不同规模的训练过程

6. **(f) 性能雷达图**
   - 3维综合评估（完成率、低时延、奖励）
   - 展示中间配置的性能

### 图表2：柱状图详细对比

**文件名**: `vehicle_sweep_bar_comparison_{timestamp}.png`  
**尺寸**: 12×5英寸，300 DPI  
**格式**: 1×2布局

#### 包含：
1. 时延柱状图 - 带数值标注
2. 完成率柱状图 - 带数值标注

---

## 🚀 使用方法

### 基本用法（默认生成图表）

```bash
# 指定车辆数列表
python experiments/run_td3_vehicle_sweep.py --vehicles 8 12 16 20 24 --episodes 800

# 使用范围生成
python experiments/run_td3_vehicle_sweep.py --vehicle-range 8 24 4 --episodes 500
```

**输出**：
```
results/experiments/td3_vehicle_sweep/
├── td3_vehicle_sweep_summary_{timestamp}.json       # JSON数据
├── td3_vehicle_sweep_summary_{timestamp}.md         # Markdown报告
├── vehicle_sweep_comparison_{timestamp}.png         # 综合对比图（6子图）
└── vehicle_sweep_bar_comparison_{timestamp}.png     # 柱状图对比
```

### 禁用图表生成

```bash
# 如果只想要数据，不要图表
python experiments/run_td3_vehicle_sweep.py --vehicles 8 12 16 --no-charts
```

---

## 📈 图表特性

### IEEE标准
- ✅ 300 DPI分辨率
- ✅ 色盲友好配色
- ✅ 清晰的数值标注
- ✅ 专业网格线和标签

### 自动化分析
- ✅ 自动移动平均（收敛曲线）
- ✅ 自动归一化（雷达图）
- ✅ 自动数值标注
- ✅ 自动图例生成

### 可定制
- ✅ 支持任意数量的车辆配置
- ✅ 自动适应不同的数据范围
- ✅ 自动处理缺失数据

---

## 🎓 学术应用

### 适用论文章节

#### 1. Scalability Analysis（可扩展性分析）
**推荐图表**: 综合对比图（所有6个子图）

**使用场景**:
- 展示系统如何随规模扩展
- 分析性能瓶颈
- 证明算法的可扩展性

**LaTeX示例**:
```latex
\\begin{figure*}[t]
  \\centering
  \\includegraphics[width=0.95\\textwidth]{vehicle_sweep_comparison.png}
  \\caption{Performance comparison across different vehicle counts showing 
           (a) average delay, (b) task completion rate, (c) reward evolution, 
           (d) training time, (e) convergence behavior, and (f) multi-dimensional 
           performance radar chart.}
  \\label{fig:scalability}
\\end{figure*}
```

#### 2. Parameter Sensitivity（参数敏感性）
**推荐图表**: 时延和完成率曲线

**使用场景**:
- 证明关键参数的影响
- 找到最优配置点
- 分析性能权衡

#### 3. System Performance（系统性能）
**推荐图表**: 柱状图对比

**使用场景**:
- 简洁展示关键指标
- 直观对比不同配置
- 适合摘要或结论

---

## 💡 图表解读指南

### 1. 时延曲线

**期望趋势**:
- ✅ 随车辆数增加而增加（更多任务）
- ✅ 增长率逐渐放缓（系统优化生效）
- ❌ 急剧上升（系统过载）

### 2. 完成率曲线

**期望趋势**:
- ✅ 保持在90%以上（系统稳定）
- ✅ 随车辆数略有下降但可控
- ❌ 大幅下降（系统无法处理负载）

### 3. 收敛性对比

**观察重点**:
- ✅ 不同规模的收敛速度
- ✅ 最终性能水平
- ✅ 训练稳定性（曲线平滑度）

### 4. 雷达图

**解读方法**:
- 面积越大越好
- 形状越对称越好（各维度均衡）
- 显示综合性能概况

---

## 🔧 高级用法

### 自定义车辆数范围

```bash
# 从8到32，步长为4
python experiments/run_td3_vehicle_sweep.py --vehicle-range 8 32 4 --episodes 500
# 结果：8, 12, 16, 20, 24, 28
```

### 快速测试（少量episodes）

```bash
# 快速验证，每个配置只训练50轮
python experiments/run_td3_vehicle_sweep.py --vehicles 8 12 16 --episodes 50
```

### 长期实验（高质量）

```bash
# 高质量结果，每个配置训练800轮
python experiments/run_td3_vehicle_sweep.py --vehicles 8 12 16 20 24 --episodes 800
```

---

## 📊 数据格式

### JSON输出示例

```json
[
  {
    "num_vehicles": 8,
    "state_dim": 82,
    "episodes": 800,
    "training_time_hours": 0.45,
    "avg_step_reward": -1.234,
    "avg_delay": 1.567,
    "avg_completion": 0.967,
    "episode_rewards": [-300.5, -295.2, ...],
    "episode_metrics": {
      "avg_delay": [2.1, 1.9, ...],
      ...
    }
  },
  ...
]
```

### Markdown报告示例

```markdown
# TD3 不同车辆数量实验结果

| Vehicles | State Dim | Episodes | Training Hours | Avg Step Reward | Avg Delay (s) | Completion Rate |
| -------- | --------- | -------- | --------------- | ---------------- | ------------- | ---------------- |
| 8        | 82        | 800      | 0.450           | -1.2340          | 1.5670        | 96.70%           |
| 12       | 112       | 800      | 0.623           | -1.4560          | 1.8900        | 95.40%           |
| 16       | 142       | 800      | 0.812           | -1.6780          | 2.1230        | 94.20%           |
```

---

## 🐛 故障排查

### 问题1：图表生成失败

**错误信息**: `[WARNING] Failed to generate charts: ...`

**解决方法**:
1. 检查matplotlib是否安装：`pip install matplotlib`
2. 检查seaborn是否安装：`pip install seaborn`
3. 数据仍会保存，只是图表生成失败

### 问题2：收敛曲线为空

**原因**: Episode数据未保存

**检查**:
- 确认训练是否真正完成
- 检查`episode_rewards`是否在结果中

### 问题3：图表太大/太小

**调整方法**:
修改`experiments/run_td3_vehicle_sweep.py`:
```python
# 修改图表尺寸
fig = plt.figure(figsize=(20, 15))  # 改为20×15英寸
```

---

## 📈 性能预期

### 实验时长估算

| 车辆数配置 | Episodes | 每个配置 | 总时长估算 |
|-----------|----------|---------|-----------|
| 3个 (8,12,16) | 200 | ~5分钟 | 15分钟 |
| 5个 (8,12,16,20,24) | 500 | ~15分钟 | 1.25小时 |
| 5个 (8,12,16,20,24) | 800 | ~25分钟 | 2小时 |
| 7个 (8,12,...,32) | 800 | ~25分钟 | 2.9小时 |

**建议**:
- 快速验证：3个配置×200 episodes
- 论文实验：5个配置×800 episodes

---

## 🎯 最佳实践

### 1. 选择合适的车辆数范围

**推荐**:
```bash
# 覆盖小、中、大规模
--vehicles 8 12 16 20 24
```

**避免**:
```bash
# 太密集（结果差异小）
--vehicles 8 9 10 11 12

# 太稀疏（缺少中间状态）
--vehicles 8 24
```

### 2. 确保统计显著性

```bash
# 每个配置足够的episodes
--episodes 500  # 最少
--episodes 800  # 推荐
```

### 3. 固定随机种子（可重复性）

```bash
# 使用固定种子
--seed 42

# 多次实验使用不同种子
for seed in 42 43 44; do
  python experiments/run_td3_vehicle_sweep.py --vehicles 8 12 16 --seed $seed
done
```

---

## 📝 论文撰写建议

### 图表标题示例

```latex
\\caption{Scalability analysis of TD3 algorithm under varying vehicle counts. 
         The system demonstrates robust performance with (a) manageable delay increase, 
         (b) stable completion rates above 94\\%, and (c) consistent convergence behavior 
         across different scales.}
```

### 描述文本示例

```
As shown in Figure X, we evaluate the scalability of our TD3-based approach 
by varying the number of vehicles from 8 to 24. The results demonstrate 
that while average delay increases from 1.57s to 2.34s (a 49% increase), 
the task completion rate remains above 94%, indicating robust system 
performance under varying network scales.
```

---

## 🚀 完整工作流示例

```bash
# 1. 运行实验（自动生成图表）
python experiments/run_td3_vehicle_sweep.py \\
  --vehicles 8 12 16 20 24 \\
  --episodes 800 \\
  --seed 42

# 2. 查看结果
cd results/experiments/td3_vehicle_sweep/

# 3. 检查图表
# vehicle_sweep_comparison_{timestamp}.png
# vehicle_sweep_bar_comparison_{timestamp}.png

# 4. 查看数据
cat td3_vehicle_sweep_summary_{timestamp}.md

# 5. 插入论文
# 复制图表到LaTeX项目
# 引用数据撰写分析
```

---

## 🎉 总结

### 核心优势

1. **自动化** - 运行实验自动生成图表
2. **专业** - 300 DPI，IEEE标准配色
3. **全面** - 6个子图展示多个维度
4. **易用** - 一行命令搞定
5. **灵活** - 支持任意车辆数配置

### 适用场景

- ✅ 可扩展性分析
- ✅ 参数敏感性研究
- ✅ 系统性能评估
- ✅ Baseline对比
- ✅ 论文实验

---

**更新**: 2025-10-08  
**维护**: VEC边缘计算团队  
**版本**: v1.0
