# HTML报告优化 - 快速使用指南

## 🎯 一分钟了解

HTML报告现在包含 **Per-Step级别** 的训练曲线图，相比之前的Episode级别图表，能够更细致地展示算法的学习动态。

---

## 🚀 快速开始

### 生成带Per-Step图表的HTML报告

```bash
# 从训练结果生成报告
python generate_html_report.py results/single_agent/td3/training_results_20251008_125531.json --open
```

**就这么简单！** 报告会自动：
1. 搜索训练目录下的Per-Step图表（`training_overview.png`, `objective_analysis.png`）
2. 将它们嵌入到HTML报告中
3. 在浏览器中打开完整报告

---

## 📊 报告内容

### 新增：Per-Step级别曲线图

**1. 训练总览** (`training_overview.png`)
- ✅ 平均每步奖励（Avg Reward per Step）
- ✅ 任务完成率 + 平均时延
- ✅ 能耗趋势 + 缓存命中率
- ✅ 收敛性分析

**2. 优化目标分析** (`objective_analysis.png`)
- ✅ 时延演化曲线
- ✅ 能耗演化曲线
- ✅ 加权目标函数：2.0×时延 + 1.2×能耗

### 保留：Episode级别汇总图

- 奖励演化曲线
- 关键性能指标演化
- 能耗与时延权衡分析

---

## 🔑 关键区别

| | Per-Step级别 | Episode级别 |
|---|---|---|
| **显示内容** | 每步平均性能 | 每轮汇总性能 |
| **适用场景** | 细节分析、收敛研究 | 整体评估、算法对比 |
| **数据粒度** | 高（200步/episode） | 低（1个值/episode） |

**举例**：
- Episode奖励：-320.5（整轮总和）
- Per-Step奖励：-1.6（-320.5 / 200步）

---

## 📁 文件结构

训练后的目录结构：

```
results/single_agent/td3/
├── training_results_20251008_125531.json    # 训练数据
├── training_overview.png                     # Per-Step总览图 ⭐
├── objective_analysis.png                    # 优化目标图 ⭐
├── training_report_20251008_151627.html     # HTML报告（包含所有图表）
└── best_td3_single_agent_xxx.pth            # 最佳模型
```

---

## 💡 使用技巧

### 1. 批量生成报告

```bash
# 为所有算法生成报告
python generate_html_report.py results/single_agent/*/training_results_*.json
```

### 2. 查看最新结果

```bash
# Windows
python generate_html_report.py results\single_agent\td3\training_results_*.json --open

# Linux/Mac
python generate_html_report.py results/single_agent/td3/training_results_*.json --open
```

### 3. 训练时自动生成

```bash
# 训练脚本会自动生成所有图表和报告
python train_single_agent.py --algorithm TD3 --episodes 200
```

---

## ❓ 常见问题

### Q: Per-Step图表没有显示？

**A**: 确保训练过程完成且图表已生成：

```bash
# 检查图表是否存在
dir results\single_agent\td3\*.png    # Windows
ls results/single_agent/td3/*.png     # Linux/Mac
```

如果不存在，运行一次快速训练：
```bash
python train_single_agent.py --algorithm TD3 --episodes 10
```

### Q: HTML文件很大（5-7 MB）？

**A**: 这是正常的，因为Per-Step图表以base64格式嵌入。
- **优点**：单文件自包含，无需外部依赖
- **缺点**：文件体积增大

如果需要减小文件大小，可以选择不嵌入Per-Step图表（直接查看PNG文件）。

### Q: 浏览器加载慢？

**A**: 第一次加载可能需要2-3秒（base64解码）。
- **建议**：使用Chrome或Firefox（性能更好）
- **替代方案**：直接查看PNG图表文件

---

## 📝 完整文档

详细的技术文档和实现细节，请参考：
- 📄 **`docs/html_report_optimization.md`** - 完整优化说明
- 📄 **`docs/academic_experiments_guide.md`** - 学术实验指南

---

## ✅ 总结

**一句话概括**：HTML报告现在既有宏观的Episode级别趋势，也有微观的Per-Step级别细节，为算法分析提供双层次视角。

**推荐用法**：
1. 训练后自动生成报告（`train_single_agent.py`）
2. 在浏览器中查看HTML报告（包含所有图表）
3. 论文图表直接使用PNG文件（高分辨率）

---

**最后更新**: 2025-10-08  
**版本**: v1.0  
**作者**: VEC边缘计算团队
